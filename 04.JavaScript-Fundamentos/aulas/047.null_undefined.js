let valor; // undefined - valor não definido

console.log(valor);

valor = null; // null - Ausência de qualquer valor e tipo.

// A diferença maior entre os dois é que undefined só deve ser
// compreendido no momento da criação de uma variável. Dessa forma
// não devemos atribuir undefined explicitamente pois não é uma boa
// prática

// O operador null deve ser utilizado para quando se quer expressar
// ausÊncia de valor de qualquer tipo (seja objeto, string, número, etc...).
// O null é muito útil em situações de validação de dados em requisições de
// bancos de dados.

console.log(valor);

const produto = {};
